export enum ReviewStatus {
  Scheduled = "scheduled",
  Completed = "completed",
  Missed = "missed",
  Cancelled = "cancelled",
}

export enum ReviewOutcome {
  Success = "success",
  Partial = "partial",
  Failure = "failure",
}
